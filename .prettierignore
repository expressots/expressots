# Generated by pnpm. Prettier's quote style differs from pnpm's, so formatting
# it rewrites all ~13k lines and pnpm reverts them on the next install —
# permanent churn. lint-staged matches *.yaml, so it must be ignored here.
pnpm-lock.yaml

# Build output
bin/
dist/
lib/
coverage/
.turbo/

# Vendored / fetched content
node_modules/
