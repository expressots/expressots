{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib/**", "dist/**", "bin/**"]
    },
    "test": {
      "dependsOn": ["^build", "build"]
    },
    "lint": {},
    "format": {}
  }
}
