/**
 * Metadata keys for guard system
 * These are used by the guard decorators and registry
 */
export const GUARD_METADATA_KEY = {
  guard: "expressots:guard",
  controllerGuards: "expressots:controller-guards",
  methodGuards: "expressots:method-guards",
};
