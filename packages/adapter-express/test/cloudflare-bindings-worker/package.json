{
  "name": "@expressots/cloudflare-bindings-worker-fixture",
  "version": "0.0.0",
  "private": true,
  "description": "Acceptance suite and bundle gate for cloudflareBindings(), run on workerd. Standalone on purpose: Wrangler and workerd stay out of the monorepo lockfile and are installed only by the dedicated CI job.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "measure": "node measure-bundles.mjs"
  },
  "dependencies": {
    "@expressots/adapter-express": "link:../.."
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.5.3",
    "vitest": "^4.1.11",
    "wrangler": "4.120.0"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "esbuild",
      "workerd"
    ]
  },
  "packageManager": "pnpm@10.14.0"
}
