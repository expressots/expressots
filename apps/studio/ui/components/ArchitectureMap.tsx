/**
 * Architecture Map - re-export wrapper.
 *
 * The implementation lives in `architecture/ArchitectureView.tsx`. This
 * file exists solely to keep the lazy-import path in `App.tsx` stable
 * (no changes needed there).
 */
export { ArchitectureView as ArchitectureMap } from './architecture/ArchitectureView';
